info:
  name: LOGIN
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/login"
  body:
    type: json
    data: |-
      {
        "email": "faletioluwaseyisam@gmail.com",
        "password": "olusam123"
      }
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: http://localhost:8000/api

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
